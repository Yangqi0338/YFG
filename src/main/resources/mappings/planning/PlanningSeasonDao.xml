<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="PlanningSeasonDao">
	<!--企划-产品季
	创建人：lxl
	邮箱：lxl.fml@gmail.com
	创建时间：2023-4-1 11:13:38
	版本号：1.0
-->
	<!-- *******************************************自定义方法区  不替换的区域【other_start】***************************************** -->
	<select id="countByQc" resultType="java.lang.Integer" parameterType="queryCondition">
		select  count(id)
		from t_planning_season
		<include refid="common.whereClause"></include>
		<if test="groupByClause != null">
			group by ${groupByClause}
		</if>
		<if test="orderByClause != null">
			order by ${orderByClause}
		</if>
	</select>

	<!-- *******************************************自定义方法区 【other_end】***************************************** -->
	<!-- *******************************************公用方法区(不可修改)***************************************** -->
	<!-- 公用返回值 与数据库字段保持一致即可，同时实体(驼峰命名)必须包含所有字段 -->
	<sql id="base_column">
		id,company_code,name,year,season,month,create_id,create_name,create_date,update_id,update_name,update_date,del_flag,status,remarks
	</sql>
	<!--  方法返回：成功条数。     方法名称： 新增          方法参数：list集合 -->
	<insert id="insert" parameterType="java.util.List">
		insert into t_planning_season (
		<include refid="base_column"/>
		) values
		<foreach collection="list" item="t" index="index" separator=",">
			(  #{t.id,jdbcType=VARCHAR},#{t.companyCode,jdbcType=VARCHAR},#{t.name,jdbcType=VARCHAR},#{t.year,jdbcType=VARCHAR},#{t.season,jdbcType=VARCHAR},#{t.month,jdbcType=VARCHAR},#{t.createId,jdbcType=VARCHAR},#{t.createName,jdbcType=VARCHAR},#{t.createDate,jdbcType=TIMESTAMP},#{t.updateId,jdbcType=VARCHAR},#{t.updateName,jdbcType=VARCHAR},#{t.updateDate,jdbcType=TIMESTAMP},#{t.delFlag,jdbcType=CHAR},#{t.status,jdbcType=VARCHAR},#{t.remarks,jdbcType=VARCHAR}
			)
		</foreach>
	</insert>
	<!--
        方法返回：成功条数。
        方法名称： 按list更新
        方法参数：list集合，id不为空，其他字段为空也修改，使用场景一般先查询  或更改明细的几个字段
    -->
	<update id="updateList" parameterType="java.util.List">
		<foreach collection="list" item="t" index="index" separator=";">
			update t_planning_season
			<set>
					id = #{t.id,jdbcType=VARCHAR},
					company_code = #{t.companyCode,jdbcType=VARCHAR},
					name = #{t.name,jdbcType=VARCHAR},
					year = #{t.year,jdbcType=VARCHAR},
					season = #{t.season,jdbcType=VARCHAR},
					month = #{t.month,jdbcType=VARCHAR},
					create_id = #{t.createId,jdbcType=VARCHAR},
					create_name = #{t.createName,jdbcType=VARCHAR},
					create_date = #{t.createDate,jdbcType=TIMESTAMP},
					update_id = #{t.updateId,jdbcType=VARCHAR},
					update_name = #{t.updateName,jdbcType=VARCHAR},
					update_date = #{t.updateDate,jdbcType=TIMESTAMP},
					del_flag = #{t.delFlag,jdbcType=CHAR},
					status = #{t.status,jdbcType=VARCHAR},
					remarks = #{t.remarks,jdbcType=VARCHAR},
			</set>
			where id = #{t.id,jdbcType=BIGINT}

		</foreach>
	</update>

	<!--
        方法名称： 按条件更新
        方法参数：条件构造器（构造器中T对象所有不为空属性为修改后的值）。
        方法返回：成功条数。
    -->
	<update id="update" parameterType="queryCondition">
		update t_planning_season
		<set>
				<if test="t.id != null">
					id = #{t.id
					,jdbcType=VARCHAR},
				</if>
				<if test="t.companyCode != null">
					company_code = #{t.companyCode
					,jdbcType=VARCHAR},
				</if>
				<if test="t.name != null">
					name = #{t.name
					,jdbcType=VARCHAR},
				</if>
				<if test="t.year != null">
					year = #{t.year
					,jdbcType=VARCHAR},
				</if>
				<if test="t.season != null">
					season = #{t.season
					,jdbcType=VARCHAR},
				</if>
				<if test="t.month != null">
					month = #{t.month
					,jdbcType=VARCHAR},
				</if>
				<if test="t.createId != null">
					create_id = #{t.createId
					,jdbcType=VARCHAR},
				</if>
				<if test="t.createName != null">
					create_name = #{t.createName
					,jdbcType=VARCHAR},
				</if>
				<if test="t.createDate != null">
					create_date = #{t.createDate
					,jdbcType=TIMESTAMP},
				</if>
				<if test="t.updateId != null">
					update_id = #{t.updateId
					,jdbcType=VARCHAR},
				</if>
				<if test="t.updateName != null">
					update_name = #{t.updateName
					,jdbcType=VARCHAR},
				</if>
				<if test="t.updateDate != null">
					update_date = #{t.updateDate
					,jdbcType=TIMESTAMP},
				</if>
				<if test="t.delFlag != null">
					del_flag = #{t.delFlag
					,jdbcType=CHAR},
				</if>
				<if test="t.status != null">
					status = #{t.status
					,jdbcType=VARCHAR},
				</if>
				<if test="t.remarks != null">
					remarks = #{t.remarks
					,jdbcType=VARCHAR},
				</if>
		</set>
		<include refid="common.whereClause"></include>
	</update>

	<!--
        方法名称： 按条件删除
        方法参数：条件构造器。
        方法返回：成功条数。
    -->
	<delete id="delete" parameterType="queryCondition">
		delete from t_planning_season
		<include refid="common.whereClause"></include>
	</delete>

	<select id="selectCount" resultType="java.lang.Integer" parameterType="queryCondition">
		select  count(${countByClause})
		<include refid="base_column"/>
		from t_planning_season
		<include refid="common.whereClause"></include>
		<if test="groupByClause != null">
			group by ${groupByClause}
		</if>
		<if test="orderByClause != null">
			order by ${orderByClause}
		</if>
	</select>

	<!--
        方法名称： 按条件查询
        方法参数：条件构造器(可分组和排序)。
        方法返回：返回单个对象或集合。
    -->
	<select id="select" resultType="com.base.sbc.module.planning.entity.PlanningSeason" parameterType="queryCondition">
		select <include refid="base_column"/> from t_planning_season
		<include refid="common.whereClause"></include>
		<if test="groupByClause != null">
			group by ${groupByClause}
		</if>
		<if test="orderByClause != null">
			order by ${orderByClause}
		</if>
	</select>
</mapper>

<!-- /******************************************************************************
 * Copyright (C) 2018 广州尚捷科技有限责任公司
 * All Rights Reserved.
 * 本软件为公司：广州尚捷科技有限责任公司   开发研制。未经本站正式书面同意，其他任何个人、团体
 * 不得使用、复制、修改或发布本软件.
 ***************************************************************************** -->

