spring:
  application:
    name: pdmRemote
  cloud:
    nacos:
      config:
        server-addr: 8.140.186.130:8848   # 配置中心地址
        namespace: dev
        file-extension: yml # 假如不配置，则nacos远程配置文件的后缀为.properties
logging:
  level:
    #基础包
    com.base.sbc.pdm.mapper: debug
    #指定报的日志级别
    org.springframework: info
