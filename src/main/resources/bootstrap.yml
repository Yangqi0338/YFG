spring:
  application:
    name: pdm
  cloud:
    nacos:
      config:
        server-addr: 10.8.252.11:8848   # 配置中心地址
        namespace: dev
        file-extension: yml # 假如不配置，则nacos远程配置文件的后缀为.properties
logging:
  level:
    #基础包
    com.base.sbc.pdm.mapper: debug
    #指定报的日志级别
    org.springframework: info
